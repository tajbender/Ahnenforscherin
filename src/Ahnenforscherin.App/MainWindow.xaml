<Window x:Class="Ahnenforscherin.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Ahnenforscherin.App"
        xmlns:views="using:Ahnenforscherin.App.Views"
        Title="Ahnenforscherin">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>

        <NavigationView x:Name="NavView"
                        PaneDisplayMode="Left"
                        SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Workbench"
                                    Icon="Home"
                                    Tag="workbench" />

                <NavigationViewItem Content="Settings"
                                    Icon="Setting"
                                    Tag="settings" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>

</Window>
